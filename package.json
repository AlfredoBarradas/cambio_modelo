{
  "name": "mi-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/main.jsx --bundle --outfile=dist/bundle.js --loader:.jsx=jsx && mkdir -p dist && cp public/index.html dist/index.html"
  },
  "dependencies": {
    "react": "^18.3.0",
    "react-dom": "^18.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}